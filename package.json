{
  "name": "xcodelog-to-md",
  "main": "bin/xcodelog-to-md.js",
  "version": "1.0.0",
  "description": "A command line app for turning xcode output to markdown",
  "license": "MIT",
  "repository": "davidahouse/xcodelog-to-md",
  "preferGlobal": true,
  "bin": {
    "xcodelog-to-md": "bin/xcodelog-to-md.js"
  },
  "dependencies": {
    "commander": "^5.1.0",
    "line-reader": "^0.4.0"
  },
  "devDependencies": {
    "eslint": "^8.12.0",
    "eslint-config-prettier": "^8.5.0"
  },
  "scripts": {
    "lint": "eslint $(find . -name \"*.js\" -not -path \"./node_modules/*\" -not -path \"./public/*\")",
    "lint-fix": "eslint --fix $(find . -name \"*.js\" -not -path \"./node_modules/*\" -not -path \"./public/*\")"
  }
}
